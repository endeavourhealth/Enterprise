/*global define*/

define([
        'plugins/router'
    ],
    function (
        router
    ) {
        "use strict";

        var vm = {
            router: router
        };

        return vm;
    }
);